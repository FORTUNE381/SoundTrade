.dashboard-content {
  max-width: 1100px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 32px;
}

.dashboard-panel {
  background-color: transparent;
}

.dashboard-panel h2 {
  color: var(--navy);
  font-size: 1.8rem;
  margin-bottom: 20px;
  text-align: center;
}